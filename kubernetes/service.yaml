apiVersion: v1
kind: Service
metadata:
  name: mlops-unified-service
  labels:
    app: mlops-unified
spec:
  type: LoadBalancer
  selector:
    app: mlops-unified
  ports:
  - name: api-port
    port: 8000
    targetPort: 8000
  - name: ui-port
    port: 8501
    targetPort: 8501
